# BudgetPro Android

Capacitor Android wrapper for the deployed BudgetPro application. A custom `NativeApp` plugin reports the installed APK version and starts signed APK updates.

The production APK is built and signed by `.github/workflows/native-release.yml`.
